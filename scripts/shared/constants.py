from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parents[2]
SCRIPTS = REPO_ROOT / "scripts"
LIBFLO_DIR = REPO_ROOT / "libflo"
REFLO_DIR = REPO_ROOT / "reflo"
DEMO_DIR = REPO_ROOT / "Demo"
EXAMPLES_DIR = REPO_ROOT / "Examples"